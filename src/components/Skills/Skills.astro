---
---

<section class="skills" id="skills">
  <div class="container">
    <h2 class="section-title" data-i18n="skills.title">//Perfil  y Habilidade</h2>
    
    <div class="profile-grid">
      <div class="profile-card whoami-card">
        <h3 data-i18n="skills.whoami">$ whoami</h3>
        <p data-i18n="skills.whoamiText">Técnico en sistemas con habilidades en creación, reparación y mantenimiento de hardware y software. Experiencia práctica en desarrollo web y configuración de equipos de red (switches/routers) mediante SSH. Habilidades blandas desarrolladas como monitor académico.</p>
        <button id="what-am-i-btn" class="what-am-i-btn" data-i18n="skills.whatAmI">¿Qué soy, entonces?</button>
      </div>

      <div class="profile-card">
        <h3 data-i18n="skills.objective">$ cat profile.txt</h3>
        <p data-i18n="skills.objectiveText">Desarrollar, mantener y dar soporte de software, red de internet o hardware para una empresa o cliente.</p>
      </div>
    </div>

    <dialog id="quote-dialog" class="quote-dialog">
      <div class="quote-content">
        <button id="close-dialog" class="close-btn">×</button>
        <div class="terminal-header">
          <span class="terminal-dot red"></span>
          <span class="terminal-dot yellow"></span>
          <span class="terminal-dot green"></span>
        </div>
        <blockquote data-i18n="skills.descartesQuote">
          «¿Qué soy, entonces? Una cosa que piensa. Y ¿qué es una cosa que piensa? Es una cosa que duda, que entiende, que afirma, que niega, que quiere, que no quiere, que imagina también, y que siente.»
        </blockquote>
        <p class="citation" data-i18n="skills.descartesCitation">Descartes, R. (1641). Meditationes de prima philosophia.</p>
      </div>
    </dialog>

    <div class="skills-grid">
      <div class="skill-category">
        <h3 data-i18n="skills.categories.webDev">Desarrollo Web</h3>
        <p class="skill-tags">React | Next | Astro | JavaScript | CSS Vanilla</p>
      </div>

      <div class="skill-category">
        <h3 data-i18n="skills.categories.programming">Programación</h3>
        <p class="skill-tags">Java | Python</p>
      </div>

      <div class="skill-category">
        <h3 data-i18n="skills.categories.networking">Redes</h3>
        <p class="skill-tags">Cisco (L2/L3) | Protocols</p>
      </div>

      <div class="skill-category">
        <h3 data-i18n="skills.categories.systems">Sistemas y Herramientas</h3>
        <p class="skill-tags">Linux | Windows | VirtualBox | Figma | Packet Tracer</p>
      </div>

      <div class="skill-category">
        <h3 data-i18n="skills.categories.security">Seguridad</h3>
        <p class="skill-tags">Firewalls | Vulnerability | Analysis | Best Practices</p>
      </div>

      <div class="skill-category">
        <h3 data-i18n="skills.categories.hardware">Hardware</h3>
        <p class="skill-tags">Assembly | Repair | Diagnostics</p>
      </div>
    </div>
  </div>
</section>

<style>
  .skills {
    min-height: 100vh;
    padding: 4rem 2rem;
    position: relative;
    z-index: 1;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .profile-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .profile-card {
    background: rgba(0,0,0,0.5);
    border: 1px solid var(--color-matrix-primary);
    border-radius: 8px;
    padding: 1.5rem;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    position: relative;
  }

  .whoami-card {
    padding-bottom: 3.5rem;
  }

  .profile-card:hover {
    background: rgba(0, 55, 20, 0.1);
    box-shadow: 0 0 20px rgba(57, 255, 20, 0.2);
    transform: translateY(-4px);
  }

  .profile-card h3 {
    font-family: var(--font-matrix);
    color: var(--color-matrix-primary);
    margin: 0 0 1rem 0;
    font-size: 1.2rem;
  }

  .profile-card p {
    color: #ddd;
    line-height: 1.6;
    margin: 0;
  }

  .what-am-i-btn {
    position: absolute;
  bottom: 1rem;
  right: 1rem;
  background: transparent;
  border: 0px solid var(--color-matrix-primary);
  color: var(--color-matrix-primary);
  padding: 0.5rem 1rem;
  font-family: var(--font-terminal);
  font-size: 0.9rem;
  cursor: pointer;
  transition: all 0.3s ease;
  border-radius: 4px;
  text-decoration-line: underline;
  }

  .what-am-i-btn:hover {
    text-shadow: 0 0 10px rgba(57, 255, 20, 0.4);
  }

  .quote-dialog {
    background: transparent;
    border: none;
    padding: 0;
    margin: auto;
    max-width: 600px;
    width: 90%;
    color: #ddd;
  }

  .quote-dialog::backdrop {
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(5px);
  }

  .quote-content {
    background: rgba(10, 10, 10, 0.95);
    border: 1px solid var(--color-matrix-primary);
    border-radius: 8px;
    padding: 2rem;
    position: relative;
    box-shadow: 0 0 30px rgba(57, 255, 20, 0.2);
  }

  .close-btn {
    position: absolute;
    top: 0.5rem;
    right: 1rem;
    background: none;
    border: none;
    color: #ff5f56;
    font-size: 2rem;
    cursor: pointer;
    z-index: 10;
  }

  .terminal-header {
    display: flex;
    gap: 8px;
    margin-bottom: 1.5rem;
  }

  .terminal-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .red { background-color: #ff5f56; }
  .yellow { background-color: #ffbd2e; }
  .green { background-color: #27c93f; }

  blockquote {
    font-family: 'Jacquard 12', system-ui, sans-serif;
    font-size: 2rem;
    line-height: 1.4;
    color: var(--color-matrix-primary);
    margin: 0 0 1.5rem 0;
    border-left: 4px solid var(--color-matrix-primary);
    padding-left: 1.5rem;
  }

  .citation {
    font-family: var(--font-terminal);
    font-size: 1rem;
    color: #aaa;
    text-align: right;
    font-style: italic;
    margin: 0;
    padding-top: 1rem;
    border-top: 1px solid rgba(57, 255, 20, 0.3);
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .skill-category {
    background: rgba(0,0,0,0.5);
    border: 1px solid var(--color-matrix-primary);
    border-radius: 8px;
    padding: 1.5rem;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    position: relative;
    cursor: grab;
  }

  .skill-category:hover {
    background: rgba(0, 55, 20, 0.1);
    box-shadow: 0 0 20px rgba(57, 255, 20, 0.2);
    transform: translateY(-4px);
  }

  .skill-category:active {
    cursor: grabbing;
  }

  .skill-category h3 {
    font-family: var(--font-matrix);
    color: var(--color-matrix-primary);
    margin: 0 0 1rem 0;
    font-size: 1.2rem;
  }

  .skill-tags {
    font-family: var(--font-terminal);
    color: #ddd;
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 768px) {
    .skills {
      padding: 3rem 1rem;
    }
  }

  .section-title {
    font-family: var(--font-matrix);
    font-size: clamp(2rem, 4vw, 3rem);
  }
</style>

<script>
  function updateContent() {
    // i18n logic temporarily disabled due to missing languages definition
    /*
    const lang = document.documentElement.lang || 'es';
    const t = languages[lang];

    document.querySelectorAll('[data-i18n]').forEach(el => {
      const key = el.getAttribute('data-i18n');
      if (key) {
        const keys = key.split('.');
        let value = t;
        for (const k of keys) {
          value = value?.[k];
        }
        if (value && typeof value === 'string') {
          el.textContent = value;
        }
      }
    });
    */
  }

  const dialog = document.getElementById('quote-dialog');
  const openBtn = document.getElementById('what-am-i-btn');
  const closeBtn = document.getElementById('close-dialog');

  if (dialog && openBtn && closeBtn) {
    openBtn.addEventListener('click', () => {
      dialog.showModal();
    });

    closeBtn.addEventListener('click', () => {
      dialog.close();
    });

    dialog.addEventListener('click', (e) => {
      const rect = dialog.getBoundingClientRect();
      const isInDialog = (rect.top <= e.clientY && e.clientY <= rect.top + rect.height &&
        rect.left <= e.clientX && e.clientX <= rect.left + rect.width);
      if (!isInDialog) {
        dialog.close();
      }
    });
  }

  function initElasticDrag() {
    const cards = document.querySelectorAll('.skill-category');
    
    cards.forEach(card => {
      const element = card;
      let isDragging = false;
      let startX = 0;
      let startY = 0;
      let initialTransform = '';
      
      const onStart = (e) => {
        isDragging = true;
        startX = e.type === 'mousedown' ? e.clientX : e.touches[0].clientX;
        startY = e.type === 'mousedown' ? e.clientY : e.touches[0].clientY;
        
        // Store initial transform if needed, though we assume 0,0 mostly
        initialTransform = element.style.transform;
        
        // Disable transition for instant drag response
        element.style.transition = 'none';
        element.style.zIndex = '10'; // Bring to front while dragging
        element.style.cursor = 'grabbing';
        // Force background to stay dark during drag (overriding hover effect)
        element.style.background = 'rgba(0,0,0,0.5)';
        
        window.addEventListener('mousemove', onMove);
        window.addEventListener('touchmove', onMove, { passive: false });
        window.addEventListener('mouseup', onEnd);
        window.addEventListener('touchend', onEnd);
      };
      
      const onMove = (e) => {
        if (!isDragging) return;
        
        // Prevent scrolling on touch devices while dragging
        if (e.type === 'touchmove') {
          e.preventDefault();
        }
        
        const currentX = e.type === 'mousemove' ? e.clientX : e.touches[0].clientX;
        const currentY = e.type === 'mousemove' ? e.clientY : e.touches[0].clientY;
        
        const deltaX = currentX - startX;
        const deltaY = currentY - startY;
        
        element.style.transform = `translate(${deltaX}px, ${deltaY}px)`;
      };
      
      const onEnd = () => {
        if (!isDragging) return;
        isDragging = false;
        
        // Spring back effect
        element.style.transition = 'transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1)';
        element.style.transform = 'translate(0, 0)';
        element.style.cursor = 'grab';
        
        // Clean up listeners
        window.removeEventListener('mousemove', onMove);
        window.removeEventListener('touchmove', onMove);
        window.removeEventListener('mouseup', onEnd);
        window.removeEventListener('touchend', onEnd);
        
        // Reset styles after animation
        setTimeout(() => {
          element.style.transition = '';
          element.style.zIndex = '';
          element.style.background = ''; // Reset background to allow hover effect again
        }, 600);
      };
      
      element.addEventListener('mousedown', onStart);
      element.addEventListener('touchstart', onStart, { passive: false });
    });
  }

  document.addEventListener('DOMContentLoaded', () => {
    updateContent();
    initElasticDrag();
  });
  window.addEventListener('languageChanged', updateContent);
</script>